/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['submitform.acedigitalsolution.com'],
    // hostname: "submitform.acedigitalsolution.com",
  },
};

export default nextConfig;
